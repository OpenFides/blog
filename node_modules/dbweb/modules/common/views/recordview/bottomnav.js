$(function () {
	$("#btnRowCount").click(function () {
		$(this).prop("disabled", true);
		$.getJSON(buildUrl() + "&action=rc", function (data) {
			rowCount = data.RowCount;
			$("#btnRowCount").text(rowCount);
			$("#btnRowCount").prop("disabled", false);
		});
	});
});
