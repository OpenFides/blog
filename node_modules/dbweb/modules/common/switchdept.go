package common

import (
	"dbweb/core"
)

type SwitchDept struct{}

func (s *SwitchDept) Get(p *core.ElementHandleArgs) {
	u := p.User
	u.SwitchDept(p.DB, p.Req.URL.Query().Get("dept"))
	p.LSession.Set("$user", u)
	p.Redirect(p.Req.URL.Query().Get("next"))
	return
}
