package common

import (
	"dbweb/core"
)

const modelTmpInTableName = "TMPINTABLE"

type modelTmpInTable struct {
	SID string `dbx:"str(50) primary key"`
	Val string `dbx:"str(1000) primary key"`
}

func init() {
	core.RegisterModel(modelTmpInTable{}, 1, false, modelTmpInTableName)
}
