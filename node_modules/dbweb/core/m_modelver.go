package core

//ModelVerName 存放model版本的表名
const ModelVerName = "MODELVER"

//ModelModelVer 存放model版本的表名
type ModelModelVer struct {
	Name    string `dbx:"STR(50) PRIMARY KEY"`
	Version int64  `dbx:"INT NOT NULL"`
}

func init() {
	RegisterModel(ModelModelVer{}, 1,false, ModelVerName)
}
