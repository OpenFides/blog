package job

import (
	"github.com/robfig/cron"
)

var jobs = cron.New()

func init() {
	jobs.Start()
}
func AddFunc(spec string, cmd func()) error {
	return jobs.AddJob(spec, cron.FuncJob(cmd))
}
func Stop() {
	jobs.Stop()
}
